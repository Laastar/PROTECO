#include<stdio.h>
#include<stlib.h>

int main()
{

  int a[2][2];

  int** a = (int**) calloc(2, sizeof(int*));
  a[0] = (int*) calloc(1, sizeof(int));
  a[1] = (int*) calloc(1, sizeof(int));

  free (a[0])
  free (a[1])
}
